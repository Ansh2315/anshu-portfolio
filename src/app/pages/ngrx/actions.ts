import { createAction } from "@ngrx/store";

export const add = createAction('Addition Action');
export const subtract = createAction('Subtraction Action');
export const reset = createAction('Resset Action');